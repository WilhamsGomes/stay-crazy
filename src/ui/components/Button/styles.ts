import styled from "styled-components";

const Primary = styled.button`
  min-width: 120px;
  height: 40px;
  background: #f8330c;
  border: none;
  border-radius: 8px;
  font-weight: bold;
  padding: 10px;
`;

export const Button = {
  Primary,
};
